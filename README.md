# alrahi-custom-suite
this is the Blueprint Package for Customizing odoo in python  
This is demo for Alrahi  
Manifest is used as the Instruction Manual  
_init_.py used to run the folder as a package  
model(.py files) folder is the structure of data to be stored
view(xml files) folder is used for the interface design
